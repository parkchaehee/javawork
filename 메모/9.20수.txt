추상클래스 - 상속
abstract class A{
  일반메서드()
  abstract 추상메서드()
  final 템플릿 메서드()
}
final 이 있는class
String, Integer -> 기반클래스는 변경하면 안되므로 final로 정의됨
상수-1개
열거형 상수 - enum Level{}

인터페이스 - 상속관계아님
interface Abc{
  인터페이스 변수(상수-내부적으로 final)
  추상메서드()
}
- 클래스같은데 생성자, 필드없음
- 추상메서드(), static 정적메서드(), default 일반메서드()

예외 처리(Exception) - try ~catch