-조인

1. Oracle DBMS 방식 - oracle, mysql
- 동등 조인(EQUL JOIN)
select *
from a, b 테이블
where절 a.b칼럼 = b.b칼럼

2. 표준 방식 - 모든 DBMS
- 내부 조인(INNER JOIN)
select *
from a 테이블 (INNER) JOIN b 테이블
   on a.b칼럼 = b.b칼럼


상품, 상품리뷰, 이벤트
상품엔티티 - 상품코드, 상품명, 가격
상품후기(리뷰) 엔티티 - 리뷰번호(자동순번), 상품코드(외래키), 
	회원아이디, 리뷰내용, 작성일
이벤트 엔티티 - 이벤트 번호, 이벤트 이름, 이벤트 시작일, 이벤트 종료일

product(테이블) - product_code, product_name, price(칼럼명)
product_review - review_no, product_code, member_id, 
	content, regdate
event(테이블) - event_no, event_name, start_date, end_date



- 테이블 삭제(DDL) - ROLLBACK 불가
1. DROP TABLE 테이블명 - 테이블 자체를 삭제
2. TRUNCATE TABLE 테이블명 - 데이터만 삭제, 테이블 유지

- 데이터 삭제
DELETE FROM 테이블명 - 데이터 전체 삭제(ROLLBACK가능)
DELETE FROM 테이블명 WHERE 칼럼명(데이터 1개 삭제)

서브쿼리







시험
오라클, 스탠다드 조인
inner join






















