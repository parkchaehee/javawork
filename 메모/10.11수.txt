개념 모델링 - 개체 관계도(ER-Diagram)
-사각형: 엔티티(Entitiy), 원형:속성(Attribute), 마름모: 관계
- IE(Information Engeering) 표기: 새발(crow feet) 표기법
논리 모델링
물리 모델링
- 테이블(엔티티), 칼럼(속성), 자료형(BYTE)

** 학교 데이터 베이스
학과 - 1개(학과명, 전화번호, 사무실위치)
학생 - 여러명(학번, 이름, 나이, 성별, 주소)
1대 다(多)

'' NULL - 데이터 없음
' ' (한칸띄움) - 공백문자 들어감

1. DDL - CREATE, ALTER, DROP
2. DML - INSERT, SELECT, UPDATE, DELETE

 ** 삽입
INSERT INTO 칼럼명 VALUES (칼럼값)


 **수정
UPDATE 테이블이름 SET 칼럼 = 변경할 칼럼값
WHERE 조건절
 
 **삭제
DELETE FROM 테이블이름
WHERE 조건절

 **검색
SELECT 칼럼명 FROM 테이블명

3. 트랜잭션(Transaction) - COMMIT, ROLLBACK
4. DCK - GRABT(권한부여), REVOKE(권한뺏기)

계정 - system
khsql
CREATE USER khsql
GRANT CONNECT DBA TO khsql;


dbconnection 프로젝트
lib > Ojdbc11.jar

Connection
DriverManager
mydb
회원가입(users) - //5개 칼럼
userid, username, userpassword, userage, useremail
클래스 - 객체(get, set)
VO(Value Object), DTO(Data Transfer Object)
DAO - CRUD
게시판(boards)














